parameters:
  input_json:
    values: [ 'output/MMDS-TaskPrediction1.json' ]
  output_json:
    values: ['output/MMDS-TaskPrediction1.json']
  task:
    values ['tasks']

  bash_file:
    values: ['extract_info_from_str.sh']
  n_cpu: # if you need more GPUs, make sure n_cpu and memory are equal ton_gpu * (current value)
    values: [ 12 ]
  memory:
    values: [ 23040 ]